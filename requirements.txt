# moreStacks Banking Application Requirements

# Core dependencies (all built-in with Python 3.x)
# No external packages required!

# The application uses only Python standard library modules:
# - tkinter (GUI framework)
# - sqlite3 (database)
# - hashlib (password hashing)
# - datetime (timestamp handling)
# - csv (transaction export)
# - abc (abstract base classes)
# - typing (type hints)

# Optional: For future enhancements
# matplotlib>=3.5.0  # For charts and graphs
# reportlab>=3.6.0   # For PDF report generation
# bcrypt>=4.0.0      # For better password hashing
# pillow>=9.0.0      # For image handling in GUI
